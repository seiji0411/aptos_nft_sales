export type FAQ = {
    title: string;
    content: string;
}

export type CollectionInfo = {
    collection_address: string,
    collection_name: string,
    collection_uri: string,
    base_uri: string,
}